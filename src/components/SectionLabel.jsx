import React from 'react';

const SectionLabel = ({ children }) => (
  <p className="text-[10px] font-semibold tracking-[0.15em] text-[#6B6B6B] uppercase mb-2">
    {children}
  </p>
);

export default SectionLabel;
